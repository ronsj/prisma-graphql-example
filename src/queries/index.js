const { allUsers } = require('./allUsers')
const { postById } = require('./postById')
const { feed } = require('./feed')
const { draftsByUser } = require('./draftsByUser')

module.exports = {
  allUsers,
  postById,
  feed,
  draftsByUser,
}
